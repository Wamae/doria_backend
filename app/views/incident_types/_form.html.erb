<%= form_with(model: incident_type, local: true) do |form| %>
  <% if incident_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(incident_type.errors.count, "error") %> prohibited this incident_type from being saved:</h2>

      <ul>
        <% incident_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
